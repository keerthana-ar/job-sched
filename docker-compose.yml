services:
  aethersched:
    build: .
    container_name: aethersched-cluster
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - NODE_ENV=production
    restart: unless-stopped
